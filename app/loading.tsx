import PageLoader from "@/app/components/PageLoader";

export default function Loading() {
  return (
    <PageLoader
      label="Loading Page"
      detail="We are bringing the next screen in with fresh order and product details."
    />
  );
}
